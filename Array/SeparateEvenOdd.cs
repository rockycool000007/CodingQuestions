using System;

namespace CodingQuestions.Array
{
    class SeparateEvenOdd
    {
        static void Main()
        {
            int[] arr = { 1, 2, 3, 4, 5, 6, 7, 8, 9 };
            //var items = SeparateItems(arr);
            Console.Read();
        }

        //static (int[], int[]) SeparateItems(int[] arr)
        //{
        //    int[] oddArr;
        //    int[] evenArr;

        //    for (int i = 0; i < arr.Length; i++)
        //    {
        //        if(arr[i] % 2 == 0)
        //            evenArr.
        //    }

        //    return (oddArr, evenArr);
        //}
    }
}